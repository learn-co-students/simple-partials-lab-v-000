Classroom Info
<!-- Specs looks for _classroom.html.erb inside /students. Needs to be "classrooms/classroom" to render in specs.  -->
<%= render "classrooms/classroom" %>

We now want to call out the oldest student in the class:
<%= render "students/student" %>